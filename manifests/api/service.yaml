apiVersion: v1 
kind: Service 
metadata:
  name: cloudops-api
  namespace: cloudops-prod
  labels:
    app: cloudops-api
spec:
  type: ClusterIP
  selector:
    app: api 
  ports:
    - targetPort : 3000
      port: 3000
